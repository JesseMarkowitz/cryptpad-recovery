{
  "name": "cryptpad-recovery",
  "version": "0.3.2",
  "private": true,
  "description": "Offline CryptPad 5.1.0 recovery utility for StartOS 0.3.5.1 and 0.4.0",
  "bin": {
    "cryptpad-recover": "bin/cryptpad-recover.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "build:release": "scripts/build-release.sh"
  },
  "engines": {
    "node": ">=16"
  }
}
